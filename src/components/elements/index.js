import Button from './button'
import Container from './container'
import Divider from './divider'

export default {
  install(Vue) {
    Vue.use(Button)
    Vue.use(Container)
    Vue.use(Divider)
  }
}
